import React from 'react';

import UserName from './UserName';


const Card = () => {
    return (
        <div className="card">
            <UserName />
        </div>
    );
};

export default Card;
